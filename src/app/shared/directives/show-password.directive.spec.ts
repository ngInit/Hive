import { ShowPassword } from './show-password.directive';

describe('ShowPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPassword();
    expect(directive).toBeTruthy();
  });
});
