import { TrackPlaysShortPipe } from './track-plays-short-pipe';

describe('TrackPlaysShortPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackPlaysShortPipe();
    expect(pipe).toBeTruthy();
  });
});
