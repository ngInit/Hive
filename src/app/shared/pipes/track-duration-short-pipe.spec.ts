import { TrackDurationShortPipe } from './track-duration-short-pipe';

describe('TrackDurationShortPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackDurationShortPipe();
    expect(pipe).toBeTruthy();
  });
});
