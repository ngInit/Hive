{
  "buildCommand": "npm run build",
  "installCommand": "npm install --no-package-lock",
  "outputDirectory": "dist/Hive/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
